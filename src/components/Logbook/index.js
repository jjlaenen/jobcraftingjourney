import React, { Component } from 'react';
import ReflectionFormRead from '../Home/reflectionFormRead'

export default class HomePage extends Component {
  render() {
    return (
      <div>
<ReflectionFormRead />
      </div>
    );
  }
}